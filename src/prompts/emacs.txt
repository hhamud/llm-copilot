Can you generate my code within an emacs org mode shell like so:
#+BEGIN_SRC shell
ls
#+END_SRC

but replace "shell" with the name of language used
and place the code in between the hashes.
Here are the commands: {emacs}
